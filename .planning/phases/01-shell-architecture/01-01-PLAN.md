---
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "src/app/layout.tsx"
  - "src/app/globals.css"
  - "src/components/layout/Sidebar.tsx"
  - "src/components/layout/BottomTabBar.tsx"
  - "src/components/layout/PlayerBar.tsx"
autonomous: true
---

# Plan 01-01: Initialize Core Shell

## Goal
Scaffold the Next.js app and build the raw 100vh CSS Grid layout, establishing the Sidebar, Main View, and Player Bar partitions.

## Requirements Covered
- LAY-01
- LAY-02
- LAY-03

## Tasks

<task>
<read_first>
- .planning/phases/01-shell-architecture/01-CONTEXT.md
</read_first>
<action>
1. Initialize a new Next.js project using: `npx -y create-next-app@latest . --typescript --eslint --tailwind --app --src-dir --import-alias "@/*"`
2. Wait for initialization to complete, then run: `npm install lucide-react` to add standard UI icons.
</action>
<acceptance_criteria>
- `package.json` contains `next`, `react`, and `lucide-react`.
- `src/app/layout.tsx` exists.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/globals.css
</read_first>
<action>
Modify `src/app/globals.css` to reset the body, set rigid dimensions to avoid scrolling, and configure exactly Spotify's dark theme palette. Replace everything with:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: #000000;
    --foreground: #ffffff;
    --card: #121212;
    --card-foreground: #ffffff;
    --primary: #1ed760;
    --primary-foreground: #000000;
    --muted: #b3b3b3;
    --border: #282828;
  }
}

body {
  @apply bg-background text-foreground;
  height: 100vh;
  width: 100vw;
  overflow: hidden;
}
```
</action>
<acceptance_criteria>
- `src/app/globals.css` contains `height: 100vh;` and `overflow: hidden;` on body.
- `src/app/globals.css` contains correct theme variables snippet.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/layout.tsx
</read_first>
<action>
Modify `src/app/layout.tsx` to implement the rigid CSS grid template structure.
Remove default wrapper elements and replace the `<body>` content with:

```tsx
<body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
  <div className="h-screen w-full flex flex-col md:grid md:grid-cols-[280px_1fr] md:grid-rows-[1fr_90px]">
    <div className="hidden md:block overflow-hidden bg-black p-2">
      <Sidebar />
    </div>
    <main className="flex-1 md:overflow-y-auto bg-card rounded-md m-2 relative" id="main-scroll-area">
      {children}
    </main>
    <div className="hidden md:block col-span-2 bg-black border-t border-border">
      <PlayerBar />
    </div>
    <div className="block md:hidden border-t border-border bg-black absolute bottom-0 w-full">
      <BottomTabBar />
    </div>
  </div>
</body>
```
*Note: Make sure to import `Sidebar`, `BottomTabBar`, and `PlayerBar` from `@/components/layout/...` which you will build next.*
</action>
<acceptance_criteria>
- `src/app/layout.tsx` contains `md:grid-cols-[280px_1fr]`.
- Main view area has `md:overflow-y-auto`.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/layout.tsx
</read_first>
<action>
Create the UI components used in `layout.tsx`:
1. Create `src/components/layout/Sidebar.tsx`: Render a simple dummy column with library text.
2. Create `src/components/layout/PlayerBar.tsx`: Render a simple "Player" dummy text block that will fit 90px height exactly. 
3. Create `src/components/layout/BottomTabBar.tsx`: Render a dummy row of 3 icons (Home, Search, Library) for the mobile breakpoint using `lucide-react`.
</action>
<acceptance_criteria>
- `src/components/layout/Sidebar.tsx` contains valid export component.
- `src/components/layout/PlayerBar.tsx` contains valid export component.
- `src/components/layout/BottomTabBar.tsx` contains valid export component.
</acceptance_criteria>
</task>

## Verification
1. Open a terminal and run `npm run dev`.
2. Open the browser and validate the layout spans exactly `100vh`. The global vertical scrollbar must be disabled.
3. Validate sidebar hides on mobile breakpoints and the BottomTabBar appears instead.

## Must Haves
- [ ] Next.js app successfully initializes and runs.
- [ ] Body CSS ensures no global overflowing vertical scrollbar.
- [ ] Layout cleanly splits into Sidebar, MainView, and BottomPlayer.
- [ ] Responsive behavior transitions Sidebar into BottomTabBar on mobile.
